# Backend API Base URL
# For same-origin deployment (Option B): use '/api'
# For separate deployment: use full URL like 'http://localhost:8000/api'
VITE_API_BASE_URL=/api

# WebSocket URL
VITE_WS_URL=ws://localhost:8000

# Feature Flags (optional)
VITE_ENABLE_MUSIC_VIDEO=false
VITE_ENABLE_DEBUG=false
